import { HTMLAttributes } from 'react';
import { ChatObject } from 'react-chat-engine-advanced/dist/interfaces';
import { ChatCardStyle } from 'react-chat-engine-advanced/dist/components/MultiChatWindow/ChatList/ChatCard/styles';
export interface ChatCardProps extends HTMLAttributes<HTMLDivElement>, ChatCardStyle {
    title?: string;
    description?: string;
    timeStamp?: string;
    avatarUsername?: string;
    avatarUrl?: string | null | undefined;
    chat?: ChatObject;
    hasNotification?: boolean;
    isActive?: boolean;
    isLoading?: boolean;
    onClick?: () => void;
    renderChatCard?: (props: ChatCardProps) => JSX.Element | Element | React.FC<ChatCardProps>;
    renderAvatar?: (props: ChatCardProps) => JSX.Element | Element | React.FC<ChatCardProps>;
}
