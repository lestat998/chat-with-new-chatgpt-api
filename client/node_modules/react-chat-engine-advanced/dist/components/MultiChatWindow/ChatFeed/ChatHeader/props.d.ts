import { ReactNode } from 'react';
import { ChatHeaderStyles } from 'react-chat-engine-advanced/dist/components/MultiChatWindow/ChatFeed/ChatHeader/styles';
export interface ChatHeaderProps extends ChatHeaderStyles {
    id?: string | number;
    title?: string | ReactNode;
    description?: string;
    renderChatHeader?: (props: ChatHeaderProps) => JSX.Element | Element | React.FC<ChatHeaderProps>;
}
