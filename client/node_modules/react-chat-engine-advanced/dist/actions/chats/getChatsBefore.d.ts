import { ChatObject, UserAuthHeaders } from 'react-chat-engine-advanced/dist/interfaces';
declare type GetChatsBefore = (host: string, headers: UserAuthHeaders, before: string, chatCount: number, callback: (chats: Array<ChatObject>) => void) => void;
export declare const getChatsBefore: GetChatsBefore;
export {};
